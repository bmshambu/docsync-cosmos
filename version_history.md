# Version History

Convention: before a file is materially changed, its previous version is copied
to `archive/` with a version suffix (`app_v1.js`, `extractor_v1.py`, …). The
live file always keeps its canonical name. New files get a history entry but no
archive copy. One row per file per change set, newest first.

| Date | File | Previous version archived as | Change |
|---|---|---|---|
| 2026-07-30 | `app/config.py` · `.env` · `app/llm/query_planner.py` | (incremental) | **Configurable decomposition cap.** The hard-coded max of 3 sub-questions is now `MAX_SUBQUESTIONS` in `.env` (new `max_subquestions: int = 3` setting). `0` = no cap — the planner prompt switches its instruction from "Return AT MOST N" to "no fixed limit, split only as many times as there are genuinely distinct asks" (`_sub_limit_note()`), and the truncation uses the configured value. A hard `ABSOLUTE_MAX_SUBQUESTIONS = 10` ceiling ALWAYS applies (independent of the .env value) so a hallucinated/runaway split can't blow up retrieval fan-out even with "no cap" — LLM cost is fixed at 2 calls regardless, but each sub-question is its own retrieval pass (×2 if scoped/dual-tracked). Verified: default cap 3, cap 0 lets a 6-way split through, cap 20 and cap 0 both still bounded to 10 on a 12-way model response, missing-setting getattr fallback to 3. Prompt `.format()` brace-escaping fixed (JSON example stays single-braced in the rendered prompt). |
| 2026-07-30 | `README.md` | (incremental) | **New "Workflow overview" section.** Four separate, deliberately simple Mermaid flowcharts — one per tab (Data Prep, Community Summariser, Query Agent, Batch Q&A) — added right after the intro, before "Migration progress." Each shows only the user-facing shape of that tab's workflow (5-7 plain-language nodes, no LLM-call counts, graph_id, or Cosmos internals) so it doesn't compete with the detailed technical sections below. Rendered and visually verified via a scratch Artifact before committing to the README — all four render with no syntax errors. |
| 2026-07-30 | `app/services/batch_qa.py` | (incremental) | **Question decomposition visible in Batch CSV.** Decomposition (M3) already worked in Batch Q&A for free — `answer_questions()` calls the same shared `ask()` — but the CSV had no column showing whether/how a row was split, unlike the Query tab's "Interpreted as N questions" note. New `Sub-Questions` output column (pipe-separated parts, blank when not decomposed), populated in both the success and error-row paths. Verified: a decomposed row's column shows both parts; a plain row's is blank. |
| 2026-07-30 | `app/llm/query_planner.py` | (incremental) | **Question decomposition (M3, feature request).** The SAME planner LLM call (no extra call) now also emits `sub_questions` — when a question genuinely asks 2-3 SEPARATE things ("what are X's lenders AND what ESG standards does X follow?"), the planner splits it into up to 3 self-contained parts; a single ask still returns a 1-item list `[rewritten]` so callers never special-case "not compound." Capped to 3 defensively in code regardless of what the model returns; any parse failure or missing field falls back to `[original question]` (silent no-op, zero behaviour change). Verified: decomposition, cap-to-3, missing-field fallback, and malformed-JSON fallback all correct via stubbed-chat unit tests. |
| 2026-07-30 | `app/llm/query_agent.py` | (incremental) | **Question decomposition (M3).** `ask()` factors the existing scope/dual-track logic into `_context_for(q)` (unchanged behaviour) and runs it once per sub-question when decomposed; a NEW `_merge_subquestions` combines the N contexts into one context of the same shape everything downstream already expects — chunks/entities tagged with which sub-question(s) they support (e.g. "Q1 + Q2"), combined with any inner dual-track label (e.g. "Q2 · Oracle") so a scoped AND decomposed question's citations show both. New `_interleave` round-robins entity/relationship ID order across sub-questions before merging — **without this, `build_query_prompt`'s `[:MAX_PROMPT_ENTITIES]` slice would let sub-question 1's entities crowd out 2's and 3's entirely** once concatenated (verified this bug is real and the fix works: a synthetic 8-vs-1-vs-1 entity split survives an 8-slot cap with all three sub-questions represented). Chunks/communities/financial-table/title-matches aren't sliced downstream, so plain concat+dedup is correct for those. Still exactly 2 LLM calls total regardless of how many parts — only the deterministic retrieval fan-out scales (up to 3×, or 6× if also scoped/dual-tracked). Response carries `sub_questions` (None when not decomposed). Single-question path is behaviourally unchanged (verified via prompt-diff). |
| 2026-07-30 | `app/llm/prompts.py` | (incremental) | **Question decomposition (M3).** `build_query_prompt(sub_questions=)` renders a numbered "interpreted as N separate parts" block plus an answer-structuring rule (answer every part, each clearly separated; a gap in one part must not suppress the others) — both empty strings when not decomposed, verified byte-identical to the pre-decomposition prompt in that case. |
| 2026-07-30 | `templates/index.html`(N/A, JS-only) · `static/js/app.js` · `static/css/style.css` | (incremental) | **Question decomposition UI (M3).** New "Interpreted as N questions" note above the answer (mirrors the existing rewritten-question/dual-track note style) listing each sub-question with a Q1/Q2/Q3 tag — shown only when `sub_questions.length > 1`. The existing per-chunk "track" tag in the citation drawer already renders the new Q-labels with no UI change needed there. Verified in-browser with a stubbed 2-part answer. |
| 2026-07-30 | `README.md` | (incremental) | **New "Community summary handling" section.** Written after clarifying (in chat) that the office account's 344 existing whole-corpus summaries are a separate, safe-to-keep layer from the new per-scope communities. Documents: the two `graph_id` layers (`default` = whole-corpus/cross-scope vs `scope_<slug>` = per-scope) as a comparison table; the isolation guarantee (every community query filters `graph_id`, so per-scope builds can never touch `default`); what DOES invalidate `default` (Data Prep / "Rebuild graph" re-running Louvain corpus-wide — the per-scope build never does); the retrieval fallback rule (no scope → default; scope + built → scope's own; scope + not built → default filtered to scope); and the selective-rebuild checkbox behaviour + cost model. Step 6 of the query-pipeline reference now points to this section instead of duplicating it. |
| 2026-07-30 | `templates/index.html` · `static/js/app.js` · `static/css/style.css` | (incremental) | **Tab info popovers.** All the explanation of what each tab does lived only in README.md — a user in the app would never see it. Added a small "i" badge next to each of the 4 tab labels (wrapped in `.tab-wrap` so a nested-button issue is avoided — the info button is a sibling, not nested, inside the tab button); click toggles a short plain-English popover (mirrors the README's per-tab description, compressed to 2-3 sentences) positioned below the icon with a pointer arrow. Closes on outside click, Escape, or clicking another tab's icon; clicking inside the popover doesn't close it. Verified: opening an icon does NOT switch tabs (listener is a sibling button, not delegated from a parent `.tab`), correct text per tab, outside-click closes. |
| 2026-07-30 | `templates/index.html` · `static/js/app.js` · `static/css/style.css` | (incremental) | **Selective per-scope rebuild.** The "Build all scopes" button always rebuilt+resummarised EVERY scope, even ones already built — no way to cheaply refresh just one. Added a checkbox per scope row (unbuilt scopes pre-checked, already-built scopes unchecked by default — a re-run only touches what's ticked) + "Select all" (tri-state: indeterminate when partially selected). Button label/enabled-state and the cost estimate react live to the selection (`updateScopeSelectState`, delegated `change` listeners attached once). The backend already accepted a `scopes` list on `/build-scopes` — this was purely a missing UI wire-up, no backend change. Verified: default state (5 unbuilt checked, 1 built unchecked), selecting only Oracle → request scoped to `{"scopes":["Oracle"]}` → only Oracle rebuilt, other 5 scopes untouched, table refreshes correctly after. |
| 2026-07-30 | `app/llm/query_agent.py` · `app/services/graph_html*.py` · `app/routers/query.py` · `static/js/app.js` · `static/css/style.css` | (incremental) | **Clickable citation entities → focused graph.** The Query tab showed "N entities" as a bare count with no way to see WHICH entities. Now: the answer carries `entity_details` (id/name/type/source_docs for the matched entities); the "N entities" pill is clickable → a drawer lists them (name + type) with an "Open interactive graph ↗" button. New `GET /api/query/entity-graph?ids=` renders a FOCUSED graph of exactly those entities + their 1-hop neighbours, cited entities highlighted (white ring, larger). `build_graph_data` gains `matched_ids=` → per-node `matched` flag; template rings/labels matched nodes. `generate_entity_graph_html(store, ids)` uses `get_relationships_for` to pull the neighbourhood (node_cap 150). Verified: focused graph renders (1 white ring for 1 cited entity + 2 neighbours), drawer lists entities + correct graph href. |
| 2026-07-29 | `templates/index.html` + `static/js/app.js` | (incremental) | **RFP scope-selector clarity (#3 follow-up).** The Batch/RFP scope selector ("Platform / Sub Service Line / Service Function") is shared markup and works for both CSV and RFP — but it is gated on a synced metadata registry and was silently omitted when absent, so it looked "missing" in the RFP flow. Added `#batch-scope-note`: when `/api/query/scopes` reports scoping unavailable, an explicit "scope filter unavailable — sync blob metadata to enable" note shows instead of nothing. Verified: registry present → grouped selector (Oracle/Sap/Technology/Advisory), note hidden. |
| 2026-07-29 | `app/services/rfp_questions.py` | — (new file) | **RFP workflow (feedback #3).** Turns an uploaded client RFP (PDF/DOCX) into the CSV-parser's shape so the whole answer pipeline is reused. `extract_paragraphs(bytes)` → ~6k-char windows (cap 40) → parallel LLM calls extract bidder questions → dedupe (whitespace/case-normalised) + cap → `to_parsed()` → `{fieldnames:['Question'], rows, question_column, count}`. The RFP is NEVER ingested into the corpus. New `build_rfp_questions_prompt` in prompts.py (extract-only, preserve wording, no answering/inventing). |
| 2026-07-29 | `app/services/batch_qa.py` | (incremental) | **RFP workflow (#3).** OUTPUT_COLUMNS += Model, Time (s), Retrieval Settings (per-question audit; Sources doubles as citations). Per-question elapsed timed with perf_counter; `_retrieval_settings_str(query_type, platform, overrides)`. `on_progress` now passes `(done, total, idx, row)` — the full updated row + its input index — so callers stream live results in input order. |
| 2026-07-29 | `app/jobs.py` | (incremental) | **RFP workflow (#3).** `Job.partial` field + `emit.set_partial(data)` (published in `run()`, persistence-safe, in `to_dict`) → factories stream incremental results into `/status` for live views and mid-run partial downloads, without changing the factory signature. |
| 2026-07-29 | `app/routers/batch.py` | (incremental) | **RFP workflow (#3).** New `POST /upload-rfp` (pdf/docx, 25 MB cap) → extract questions → same `_uploads` + preview as CSV. Run factory seeds a full live-rows list from the input and `emit.set_partial({rows,total,done,answered})` on each completion (live table). `/download` falls back to `job.partial` mid-run → `answers_partial_<id>.csv`; full `answers_<id>.csv` when done. |
| 2026-07-29 | `templates/index.html` | (incremental) | **RFP workflow (#3)** — Tab 4: source toggle (Questions CSV ↔ RFP document), adaptive file input/hint, live badge on the results card. |
| 2026-07-29 | `static/js/app.js` | (incremental on M2 `app_v4`) | **RFP workflow (#3)** — source toggle (accept/label/hint + `/upload` vs `/upload-rfp`); poll renders `job.partial` into a live-filling table (pending rows greyed) with a live badge + partial-download; shared `renderBatchTable` (adds a Time column) for live + final. |
| 2026-07-29 | `static/css/style.css` | (incremental) | **RFP workflow (#3)** — `.seg-toggle`/`.seg` source switch, pulsing `.badge.live`, `.batch-pending` row style. |
| 2026-07-29 | `app/services/graph_html_generator.py` | (incremental) | **Per-scope graph viz (feedback #5).** `build_graph_data` gains `summaries=` (preview map from the store, not md-file glob — Cosmos has no local files) and `node_cap=` (keep top-N entities by DEGREE; drop edges to capped-out nodes; `stats.total_nodes`/`capped` report truncation). Whole-corpus graph is ~22,633 nodes → froze D3 → rendered blank; capping fixes it. Header gets a `{subtitle}` slot for the scope/truncation line. |
| 2026-07-29 | `app/services/graph_html.py` | (incremental) | **Per-scope graph viz (#5).** New `generate_scope_graph_html(store, scope_value, node_cap)` renders ONE scope's sub-graph straight from the store (no temp files): filter entities to the scope's docs, keep edges among them, colour by the scope's per-scope community graph (#4) when built else the global 'default' communities. Returns HTML string; subtitle shows scope · counts · truncation · community source. |
| 2026-07-29 | `app/routers/data_prep.py` | (incremental) | **Per-scope graph viz (#5).** `GET /graph-html?scope=&limit=` now generates the viz ON DEMAND from the store (404 if no entities) instead of serving the stale ~22k-node static file. New `GET /graph-scopes` (both fields, for the picker). |
| 2026-07-29 | `app/graphs/data_prep_graph.py` | (incremental) | **Per-scope graph viz (#5).** Both HTML-generation sites (`node_generate_html`, `run_build_only`) no longer write the whole-corpus static file (`export_snapshot` + `generate_graph_html`) — dead weight now that the viz is on-demand per scope, and it was the blank artefact. Data prep / rebuild is faster; completion emit unchanged. |
| 2026-07-29 | `templates/index.html` | (incremental) | **Per-scope graph viz (#5)** — Data Prep result card: "Graph scope" dropdown (Whole corpus + grouped Platform / Service Function values) beside the graph link + a hint that the whole-corpus graph is large. |
| 2026-07-29 | `static/js/app.js` | (incremental on M2 `app_v4`) | **Per-scope graph viz (#5)** — `loadGraphScopes` fills the picker from `/graph-scopes` (reuses `fillScopeSelect` optgroups), `updateGraphHref` points the link at `/graph-html?scope=…`; called on graph-ready + existing-graph paths. |
| 2026-07-29 | `app/services/scope_communities.py` | — (new file) | **Per-scope communities (feedback #4).** Builds a SEPARATE community graph per scope value — every Platform / Sub Service Line value AND every Service Function value — with NO entity re-extraction. Per scope: filter existing entities to the scope's docs (`scoped_doc_ids(any_value=)` ∩ `source_docs`), Louvain on the sub-graph (free), summarise its communities (LLM — the only cost). `build_scope_graph` (deterministic), `build_all_scope_communities` (async, all-upfront, per-scope community cap, live `on_progress` graph→summary→scope stages, cancellable), `plan_scopes` (cheap size/cost preview), `built_scope_status` (built vs pending), `run_scope_community_build` (durable-job entry with emit). Verified on file backend: build + isolation, orchestrator progress, summary isolation per graph_id. |
| 2026-07-29 | `app/services/graph_store.py` | (incremental on M1 `graph_store_v1`) | **Per-scope communities (#4) + scope spans both fields (#2/#4).** `graph_id` param (default 'default') threaded through get/save_community_map, save/get_community_summary, list_community_summaries, get/save_graph_stats, summary_ok(_ids), get_all_summaries — so each scope's community artefacts live in their own partition and never collide. Cosmos: EVERY communities query now filters `c.graph_id=@gid` (map shards, summaries, stats, stale-shard deletes) — a missing filter would mix scopes; per-graph_id `_map_cache`. FileGraphStore: per-scope subtree `graph/scopes/<graph_id>/`. Doc registry stays GLOBAL ('default'). New `scope_graph_id(value)` slug helper + `DEFAULT_GRAPH_ID`. Also: `list_scopes()` (both fields, `field`-tagged) + `scoped_doc_ids(any_value=)` (match either field), display keeps small words lowercase. |
| 2026-07-29 | `app/llm/summarizer.py` | (incremental) | **Per-scope communities (#4)** — `summarise_corpus(graph_id=)` reads the map from and writes every summary to that graph_id; threaded through `_summarise_one`, the error-stub save, and `_update_community_map`. |
| 2026-07-29 | `app/services/retriever.py` | (incremental on M1 `retriever_v1`) | **Per-scope communities (#4) + scope spans both fields.** For global/hybrid, when a scope is active and has a per-scope community graph, read THAT graph (already scope-only — no post-filter); else fall back to the whole-corpus 'default' graph with the in-scope filter (pre-#4 behaviour, so nothing breaks before scopes are built). Result carries `community_graph_id`. `retrieve(scope_value=)` routes to `scoped_doc_ids(any_value=)` (either field). |
| 2026-07-29 | `app/routers/community.py` | (incremental) | **Per-scope communities (#4)** — `GET /scope-plan` (per-scope size + built status + cost preview), `POST /build-scopes` (durable all-scope build job, reuses `/cancel` + `/status`), `GET /scope-summaries?graph_id=`. |
| 2026-07-29 | `templates/index.html` | (incremental on M2) | **Per-scope communities (#4)** — Community tab "Per-scope communities" card: scope plan table (scope · field tag · docs · entities · built status), max-communities-per-scope input, Build-all button, live progress bar + log + Stop. Also naming (#2): scope labels → "Platform / Sub Service Line / Service Function". |
| 2026-07-29 | `static/js/app.js` | (incremental on M2 `app_v4`) | **Per-scope communities (#4)** — `loadScopePlan` (renders plan table + estimate, hidden when no registry), Build-all handler → `/build-scopes` + `pollScopeStatus` live progress → refresh plan; Stop via `/cancel`. Also (#2): `fillScopeSelect()` grouped `/scopes` optgroups. |
| 2026-07-29 | `static/css/style.css` | (incremental) | **Per-scope communities (#4)** — `.scope-plan-table` (sticky header, numeric cols), `.scope-field-tag` (platform=accent, service_function=green), `.scope-build-controls`. |
| 2026-07-29 | `app/llm/query_agent.py` | (incremental on M2 `query_agent_v2`) | **Scope spans both fields (#2)** — Track A now retrieves via `scope_value=platform` (matches Platform OR Service Function value); Track B fixed on Clients and Markets unchanged. Dual-track trigger/labels unchanged. |
| 2026-07-29 | `app/routers/query.py` | (incremental on M1 `query_router_v1`) | **Scope spans both fields (#2)** — new `GET /scopes` (both fields, grouped by `field`); `/scope-count` now matches `any_value` (either field); `GET /platforms` kept as a back-compat alias. |
| 2026-07-29 | `app/auth.py` | — (new file) | **Simple PoC login (feedback #1).** Shared-password gate: correct password mints an HMAC-signed HttpOnly cookie (stdlib only — no `itsdangerous`/session dependency); middleware 302s unauth HTML to `/login`, 401s `/api/`, opens `/login`,`/logout`,`/static`,`/health`,`/favicon`. Secret derived from the password so cookies survive restarts and rotate on password change. No-op unless `APP_PASSWORD` is set (local dev stays open). Verified: unauth HTML→302, unauth API→401, wrong pw→401 (no cookie), correct pw→302+cookie, tampered cookie→401, `/health` open, empty password→fully open. |
| 2026-07-29 | `app/config.py` | (incremental on `config_v3`) | **Login #1** — `app_password` / `app_session_hours` settings. |
| 2026-07-29 | `app/main.py` | (incremental on `main_v2`) | **Login #1** — `install_auth(app)`; `auth_enabled` passed to the index template. |
| 2026-07-29 | `templates/base.html` | (incremental) | **Login #1** — "Sign out" pill in the top bar when `auth_enabled`. |
| 2026-07-29 | `.env` | (incremental on `env_v1`) | **Login #1** — commented `APP_PASSWORD` / `APP_SESSION_HOURS` with guidance. |
| 2026-07-22 | `app/services/graph_store.py` | (git; `graph_store_v1` = pre-M1) | **2 MB cap fix.** Graph build failed at 22,633 entities — the single `community_map` Cosmos item exceeded the 2 MB item limit (`RequestEntityTooLarge`). `CosmosGraphStore` now SHARDS the map across `kind='map_shard'` items (`ensure_ascii=True` so char-splitting is byte-safe) and reassembles on read; legacy single-item maps still read. Verified: a 2.2 MB map → 2 sub-2 MB shards, byte-identical roundtrip. FileGraphStore (one JSON file) unaffected. |
| 2026-07-22 | `app/llm/client.py` | `archive/client_v1.py` | **Reasoning-model fix.** GPT-5.5 extraction failed with empty output — the model spent its ENTIRE 16k completion budget on hidden reasoning (`reasoning_tokens=16000`), leaving 0 for the JSON. Azure path now sends `reasoning_effort` (explicit kwarg) + `max_completion_tokens` when `AZURE_REASONING_EFFORT` is set, and omits `temperature` (reasoning models only allow the default). Non-reasoning deployments (gpt-4o, empty effort) keep `temperature` + `max_tokens` unchanged. |
| 2026-07-22 | `app/config.py` | `archive/config_v2.py` | **Reasoning-model fix** — new `azure_reasoning_effort` setting (""/minimal/low/medium/high; empty = don't send). |
| 2026-07-22 | `.env` | `archive/env_v1.txt` | **Reasoning-model fix** — `MAX_EXTRACT_TOKENS` 16000→32000 (cap must cover reasoning + output); documented `AZURE_REASONING_EFFORT=low` for GPT-5.x extraction/summaries. |
| 2026-07-21 | `app/llm/query_agent.py` | `archive/query_agent_v2.py` | **M2 dual-track.** When a Platform is selected, retrieval runs TWICE — Track A (selected Platform) + Track B (fixed Services_Function="Clients and Markets") — then ONE synthesis merges the two track-labeled contexts. `_merge_dual_tracks` tags each chunk/entity by track, dedups cross-track overlaps as "A + B", merges communities/financial/title-matches. Response carries `tracks` stats + per-chunk `track`. Single-track fallback when no scope / user typed the SF value. Still 2 LLM calls. |
| 2026-07-21 | `app/llm/prompts.py` | `archive/prompts_v1.py` | **M2** — chunks rendered with `[ORACLE]` / `[CLIENTS AND MARKETS]` / `[ORACLE + CLIENTS AND MARKETS]` track labels; answer rule to draw approach from the platform track and proof/credentials from the C&M track, attributing each part and citing both-track chunks once. |
| 2026-07-21 | `app/services/batch_qa.py` | (M1-era; incremental) | **M2** — `answer_questions(platform=)` scopes the whole batch dual-track; new `Content Tracks` output column. |
| 2026-07-21 | `app/routers/batch.py` | (M1-era; incremental) | **M2** — `RunRequest.platform` threaded to `answer_questions` (cleaned via query router's `_clean_platform`). |
| 2026-07-21 | `templates/index.html` | (M1 `index_v2`; incremental) | **M2** — Batch tab Platform scope selector (+ "N in scope + Clients and Markets" preview). |
| 2026-07-21 | `static/js/app.js` | `archive/app_v4.js` | **M2** — dual-track answer breakdown pills (`Oracle (n chunks, m docs) + Clients and Markets (…)`), per-chunk track tag in the drawer, batch platform dropdown + run-body `platform`. |
| 2026-07-21 | `static/css/style.css` | (incremental) | **M2** — `.track-note` / `.track-pill` (a=accent, b=green) / `.drawer-track` styles. |
| 2026-07-21 | `.gitignore` (repo root) | — | **Cache fix** — `cosmos-rag/data/` was NOT git-ignored (only root `/data/` was); at 2k docs a `git add` would have committed multi-GB of cached blobs. Now ignored, plus `cosmos-rag/*.log` and `sample_questions.csv`. Nothing was tracked yet. |
| 2026-07-21 | `app/services/extract.py` | `archive/extract_v1.py` | **Stream-in-memory** — extractors take `bytes` (docx/pptx via BytesIO, pdf via `fitz.open(stream=...)`) instead of a path. New `process_bytes` (returns chunks inline, no file read-back) + `extract_all_streamed(names, reader, ...)`. `process_file`/`extract_all` kept as path wrappers. Blob binaries are parsed from memory — never written to a local cache. |
| 2026-07-21 | `app/services/storage.py` | `archive/storage_v2.py` | **Stream-in-memory** — `AzureBlobSource` no longer downloads to `data/blob_cache`; `list_document_names()` (names only, no download) + `read_document_bytes(name)` (one blob → memory). FolderSource gets the same two methods. `get_source` drops `local_cache`. Scan + ingestion rewired to names. |
| 2026-07-21 | `app/graphs/data_prep_graph.py` | `archive/data_prep_graph_v2.py` | **Stream-in-memory** — ingestion lists names, streams each doc's bytes via `extract_all_streamed`, saves chunks straight from the result (no file read-back). Works on names throughout. |
| 2026-07-21 | `app/routers/data_prep.py` | `archive/data_prep_router_v1.py` | **Stream-in-memory** — `/scan` uses `list_document_names()`: at 2k docs scan is now a fast metadata listing (~seconds) instead of downloading every blob (multi-GB). |
| 2026-07-21 | `app/config.py` | `archive/config_v1.py` | **Stream-in-memory** — `ensure_dirs` no longer creates a blob cache dir; `blob_cache_dir` kept as a deprecated no-op for compat. |
| 2026-07-21 | `app/services/metadata.py` | — (new file) | **M1** — blob-metadata parsing (`parse_list_value` JSON arrays) + `build_doc_record` → normalized document-registry record (lowercased `platform`/`service_functions` scope arrays + title/webUrl/is_deleted/…). |
| 2026-07-21 | `app/services/storage.py` | `archive/storage_v1.py` | **M1** — `blob_metadata_map()`: one `list_blobs(include=['metadata'])` pass → `{doc_id: record}`; `_doc_id_for()` matches the pipeline's doc_id derivation. |
| 2026-07-21 | `app/services/graph_store.py` | `archive/graph_store_v1.py` | **M1** — doc registry + scoping. Base: `save/get_doc_registry`, `list_platforms` (counts, excl. deleted), `scoped_doc_ids(platform, service_function)` (untagged→all-scopes fallback), `doc_web_url`; `list_doc_titles` enriched with curated title/name. FileGraphStore → `data/graph/doc_registry.json`; CosmosGraphStore → `kind='document'` items in communities container (cached). |
| 2026-07-21 | `app/services/retriever.py` | `archive/retriever_v1.py` | **M1** — `retrieve(platform=, service_function=)`: scope = set of doc_ids intersecting entities (source_docs join), title matches, chunks (`filter_doc_ids`), communities, and the financial table. Returns `scope_doc_count`. Scope=None → today's behaviour. |
| 2026-07-21 | `app/llm/query_agent.py` | `archive/query_agent_v1.py` | **M1** — `ask(platform=)` threads scope to retrieve; citations prefer governed `webUrl` (Templafy/SharePoint) over blob SAS; response carries `platform` + `scope_doc_count`. (M2 will add the parallel Clients-and-Markets track.) |
| 2026-07-21 | `app/routers/query.py` | `archive/query_router_v1.py` | **M1** — `GET /platforms` (dropdown values + counts), `GET /scope-count?platform=` (the "N docs in scope" preview), `AskRequest.platform` (60-char cap, cleaned). |
| 2026-07-21 | `app/graphs/data_prep_graph.py` | `archive/data_prep_graph_v1.py` | **M1** — during ingestion: fetch blob metadata once, skip IsDeleted docs, write the registry for processed docs. |
| 2026-07-21 | `templates/index.html` | `archive/index_v2.html` | **M1** — Query tab Platform scope row: dropdown + free-text + "N docs in scope" preview (hidden when no registry). |
| 2026-07-21 | `static/js/app.js` | `archive/app_v3.js` | **M1** — `loadPlatformScope`, `getSelectedPlatform`, debounced `updateScopeCount` (typo→"0 in scope" amber), `platform` in ask body, "Scoped to X" answer note. |
| 2026-07-21 | `scripts/sync_metadata.py` | — (new file) | **M1** — retrofit: read blob metadata → write registry for already-extracted docs (no LLM, no re-extraction). `--verify-only` mode. |
| 2026-07-21 | `scripts/inspect_blob_metadata.py` | — (new file) | **M0 — blob metadata discovery** (read-only, no LLM) for the service-function plan. Fetches metadata in ONE `list_blobs(include=['metadata'])` pass (no per-blob calls, fast at 2k+). Reports: coverage % per key, distinct `Services_Function_Capabilities` + `Platform` values with counts, the combined lowercased scope-tag union (what M1 filters on), multi-tag distribution, `Clients and Markets` count, `Industry_Sector_Market`, `IsDeleted` count, untagged-doc count, sample rows; optional `--json` dump. Parser handles the JSON-array format (confirmed from real samples), drops `[""]`/empties, and falls back gracefully on unexpected rows. ASCII-only output for Windows-console safety. Verified against the live 100-doc container (0% coverage path) and synthetic screenshot-format metadata (populated path). |
| 2026-07-14 | `app/services/batch_qa.py` | — (new file) | **Stage C — Batch Q&A.** CSV parsing (delimiter sniffing, BOM, question-column detection with first-column fallback), batch answering through the normal planner→retrieve→synthesise pipeline with semaphore + cancel, honest status flags, output CSV serialisation. Original columns preserved; answers appended. Includes three fixes found during live testing: (1) **retry with exponential backoff** on transient LLM errors (503/429/timeout) — a single 503 was permanently losing a question's answer, forcing a re-run of the whole CSV; backoff happens outside the semaphore so other questions keep moving, and permanent errors (bad key) fail fast; (2) **`GAP` status** — retrieval finding chunks did not mean they answered the question, so non-answers ("the provided context does not specify…") were being reported as `Answered`, defeating the Status column's purpose for triage; (3) **actionable error text** — API quota exhaustion is the most common batch failure (24 questions = 48 calls) and must not read like a crash. |
| 2026-07-14 | `app/routers/batch.py` | — (new file) | Endpoints `/api/batch/upload`, `/run`, `/cancel/{id}`, `/status/{id}`, `/download/{id}`. Reuses `job_manager` (durable jobs) and the Query tab's retrieval clamping. Limits: 2 MB upload, 500 questions. |
| 2026-07-14 | `templates/index.html` | `archive/index_v1.html` | Tab 4 "Batch Q&A": upload card + preview, query-mode select, progress card with Stop & Save, results card with stats grid, download button, and answers table. |
| 2026-07-14 | `static/js/app.js` | `archive/app_v2.js` | Batch tab JS: upload → preview → run → poll → results table + CSV download. Reuses the Query tab's session retrieval settings (`getRetrievalOverrides`). Tab-switch init hook. |
| 2026-07-14 | `app/main.py` | `archive/main_v1.py` | Register `batch.router`. |
| 2026-07-14 | `static/css/style.css` | `archive/style_v1.css` | Batch answers table (sticky header, status colouring), file input styling. |
| 2026-07-14 | `sample_questions.csv` | — (new file) | Example input format for the Batch Q&A tab. |
| 2026-07-14 | `app/llm/extractor.py` | `archive/extractor_v1.py` | **Bug fix** in `purge_doc_data`: entities with NO recorded `source_docs` were being deleted as collateral when purging any document (found during remove_doc testing — cost 2 innocent entities, restored via backfill). Now only entities whose sources were exclusively the purged docs are dropped. Also affects the force re-extract path. |
| 2026-07-14 | `static/js/app.js` | `archive/app_v1.js` | Scan-UI guardrail: warns when file names containing "RFP" are about to be ingested — client RFPs are question documents and poison answer retrieval (Stage A of reviewer feedback). |
| 2026-07-14 | `scripts/remove_doc.py` | — (new file) | Remove document(s) from the graph without full re-extraction: purges entities/relationships/chunks, cleans local scratch, rebuilds graph. Shows impact preview + confirmation. Built to evict the accidentally-ingested client RFP. |
